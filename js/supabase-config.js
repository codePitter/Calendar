/**
 * supabase-config.js — Credenciales de tu proyecto Supabase.
 *
 * Dónde encontrarlas:
 *   supabase.com → tu proyecto → Settings → API
 *   • Project URL  → reemplazá YOUR_SUPABASE_URL
 *   • anon / public key → reemplazá YOUR_SUPABASE_ANON_KEY
 */
window.CalApp = window.CalApp || {};

window.CalApp.SUPABASE_CONFIG = {
  url:     'YOUR_SUPABASE_URL',
  anonKey: 'YOUR_SUPABASE_ANON_KEY',
};
